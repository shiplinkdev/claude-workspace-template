# Code Style

<!-- TODO: Describe your code style rules. Include:
     - Import order and grouping (e.g. built-ins → external → internal → relative)
     - Formatter settings (e.g. Prettier: printWidth, singleQuote, trailingComma)
     - Linter config (e.g. ESLint ruleset, key rules that differ from defaults)
     - When to write comments (e.g. only for non-obvious WHY, never for WHAT)
     - Type annotation requirements (e.g. PropTypes, TypeScript strictness level)
     - Any enforced pre-commit hooks (formatter, linter, type check) -->
