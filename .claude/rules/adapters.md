# Adapters

<!-- TODO: Describe your adapter/transformer layer. Include:
     - What adapters are responsible for (request validation, response shaping)
     - Where adapters live and how they are named
     - Common adapter utilities your team uses (e.g. null guards, array wrappers)
     - The data flow: handler → adapter → service → adapter → handler
     - When to write an adapter vs. inline transformation -->
