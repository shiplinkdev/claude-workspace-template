# Constants

<!-- TODO: Describe your constants conventions. Include:
     - Where constants live (a single file? per-domain files? co-located?)
     - Naming convention (e.g. SCREAMING_SNAKE_CASE)
     - What qualifies as a constant vs. an inline value
     - Any categories of constants that always belong in a shared location
       (e.g. API endpoint paths, status codes, UI label strings, magic numbers) -->
