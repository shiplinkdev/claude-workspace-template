# Styling

<!-- TODO: Describe your styling approach. Include:
     - CSS strategy (e.g. Tailwind, CSS Modules, styled-components, plain CSS)
     - How to handle conditional or dynamic classes
     - Custom design tokens or theme values to always use instead of raw values
     - Custom breakpoints and how they are referenced
     - Where global styles live and what belongs there
     - Any class-name utility libraries (e.g. classnames, clsx) -->
