# Components

<!-- TODO: Describe your component architecture. Include:
     - Component types and where each lives (e.g. pages, shared UI, domain-specific)
     - Naming conventions (e.g. PascalCase, file-per-component)
     - What goes in a component vs. a service vs. a utility
     - Whether components own data fetching or receive it as props
     - Required annotations (e.g. PropTypes, TypeScript interfaces, JSDoc)
     - Co-location rules (styles, tests, types next to component?) -->
