# API

<!-- TODO: Describe your API layer conventions. Include:
     - Where route handlers live and how they are named
     - The pattern for a route handler (auth check, adapter, service call, response)
     - How authentication/authorization is enforced per request
     - How request bodies and query params are accessed
     - Error response format and status code conventions
     - Any shared utilities or wrappers all handlers must use -->
