# Services

<!-- TODO: Describe your service layer conventions. Include:
     - What belongs in a service (business logic, external API calls, calculations)
     - Where services live and how they are organized (one per domain? one per resource?)
     - What services should NOT do (e.g. format HTTP responses, access request objects)
     - How services receive dependencies (injected? imported directly?)
     - Error handling conventions within services -->
