# Stack

<!-- TODO: Describe your tech stack. Include:
     - Runtime and language (e.g. Node.js 22, Python 3.12, Go 1.22)
     - Framework (e.g. Next.js 15, Django 5, gin)
     - Key libraries: state management, HTTP client, validation, date handling
     - Build and package manager (e.g. yarn, npm, pip, cargo)
     - Testing approach (e.g. Playwright E2E, Jest unit, pytest)
     - Dev tools: linter, formatter, type checker, pre-commit hooks

     Example table format:
     | Library | Purpose |
     |---------|---------|
     | axios   | HTTP client |
     | zod     | Schema validation | -->
